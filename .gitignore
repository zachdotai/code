# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
release/
out/
.vite/
.turbo/
storybook-static
bin/

# node-gyp configure artifact from a stray run at the repo root
/build/

# tsup bundled config artifacts (temporary files left behind when bundling TS configs)
*.config.bundled_*.mjs

# Environment
.env
.env.local
.envrc

# IDE
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp
*.tgz
*.log
*.car

# AI agents
MEMORY.md
.claude-config/
.agent-trigger
.posthog-code/
.claude/worktrees/
.claude/settings.local.json

# Testing
playwright-results/
playwright-report/
.playwright-cli
test-results/
*storybook.log
.session-store.json
.playwright-mcp

# Downloaded binaries
apps/code/resources/codex-acp/

# Offline voice dictation: whisper.cpp build tree + generated/downloaded artifacts
# (built via scripts/whisper-wasm; see that dir's README).
scripts/whisper-wasm/.build/
apps/code/public/whisper/
apps/web/public/whisper/
# Offline Vosk (streaming) model archive, downloaded on demand.
apps/code/public/vosk/
apps/web/public/vosk/
# Stray Emscripten compiler-probe output (safety net; build.sh keeps these in .build).
a.out.js
a.out.wasm

# Licensed fonts (downloaded from S3 during CI)
apps/code/assets/fonts/BerkeleyMono/

# Local dev skills (override shipped + remote skills in dev mode)
plugins/posthog/local-skills/

# Symlinked copies of posthog, to make developing against those APIs easier
posthog-sym

.claude/settings.local.json
CLAUDE.local.md

apps/mobile/ROADMAP.md
# Local quill tarball for ChatX thread testing (not committed)
.local-quill/
