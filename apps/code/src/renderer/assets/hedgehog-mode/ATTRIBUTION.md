# Hedgehog Mode Assets

`sprites.png` and `sprites.json` are vendored from
[@posthog/hedgehog-mode](https://github.com/PostHog/hedgehog-mode) v0.0.48 (MIT).

Re-vendor by copying `assets/sprites.{png,json}` from the package after a version bump.
