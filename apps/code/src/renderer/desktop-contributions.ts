import { container } from "@renderer/di/container";

export function registerDesktopContributions(): void {
  // Feature modules will be loaded here as UI migrates to packages/ui.
  void container;
}
