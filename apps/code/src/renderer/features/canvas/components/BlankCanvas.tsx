import { Box } from "@radix-ui/themes";

// A blank canvas surface. Intentionally empty — the starting point for canvas
// content to be built on top of.
export function BlankCanvas() {
  return <Box className="h-full w-full bg-gray-1" />;
}
